<?xml version="1.0" encoding="UTF-8"?>
<validation>
    <field name="id">
        <condition expression="#update or #changeStatus or #delete">
            <rule type="NonNullRule"/>
        </condition>
    </field>

    <field name="name">
        <condition expression="#createOrUpdate">
            <rule type="NotBlankRule"/>
            <rule type="MaxLengthRule" maxLength="32"/>
        </condition>
    </field>

    <field name="description">
        <condition expression="#createOrUpdate">
            <rule type="MaxLengthRule" maxLength="100"/>
        </condition>
    </field>

    <field name="status">
        <condition expression="#create">
            <rule type="NonNullRule"/>
            <rule type="EqualsRule" value="ACTIVE"/>
        </condition>
        <condition expression="#changeStatus">
            <rule type="NonNullRule"/>
        </condition>
    </field>

    <field name="statusDate">
        <condition expression="#changeStatus">
            <rule type="NonNullRule"/>
        </condition>
    </field>

    <field name="statusReason">
        <condition expression="#create or #update or #changeStatus">
            <rule type="MaxLengthRule" maxLength="100"/>
        </condition>
    </field>

    <field name="adminUsernameType">
        <condition expression="#create">
            <rule type="NonNullRule"/>
        </condition>
    </field>

    <field name="adminUsernameTypePhoneIndicative">
        <condition expression="#create and adminUsernameTypePhone">
            <rule type="NotBlankRule"/>
        </condition>
    </field>

    <field name="adminUsername">
        <condition expression="#create">
            <rule type="NotBlankRule"/>
            <rule type="MaxLengthRule" maxLength="16"/>
            <rule type="UniqueUsernameRule"/>
        </condition>
        <condition expression="#create and adminUsernameTypePhone">
            <rule type="NotBlankRule"/>
            <rule type="PhoneRule"/>
        </condition>
        <condition expression="#create and adminUsernameTypeEmail">
            <rule type="NotBlankRule"/>
            <rule type="EmailRule"/>
        </condition>
    </field>

    <field name="accountContacts" type="dtoList">
        <condition expression="#create">
            <rule type="NonEmptyRule"/>
            <rule type="PrincipalAccountContactRule"/>
        </condition>
        <validationConfig ref="platform/account-contact"/>
    </field>
</validation>
